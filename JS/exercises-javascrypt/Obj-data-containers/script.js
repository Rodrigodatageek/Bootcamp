// const paragraphs = document.querySelectorAll("p");
// const array_of_paragraphs = Array.from(paragraphs);
// console.log(array_of_paragraphs);

// paragraphs.forEach((paragraphs, index) => {
//   console.log(paragraphs);
// });

const library = document.querySelectorAll("li");
library.forEach((library, index) => {
  console.log(library.innerHTML);
});
